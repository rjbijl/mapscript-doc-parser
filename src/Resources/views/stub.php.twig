<?php

/**
 * Mapscript extension (version 7.0.*)
 * Parsed from documentation
 * Generated at {{ now|date('Y-m-d H:i:s') }}
 *
 * @see http://mapserver.org/mapscript/php/phpmapscript.html
 * @see {{ stub.source }}
 */

// constants
{% for constant in stub.constants %}
const {{ constant.name }} = '';
{% endfor %}

{% for function in stub.functions %}
{% include 'partials/signature.php.twig' with {'item': function} %}
function {{ function.name }}({{ function.argumentList }}) {}

{% endfor %}